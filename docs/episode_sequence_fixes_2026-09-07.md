# Исправления последовательности эпизода

Статус: **success** для восьми пунктов проверки `episode_sequence_review_2026-09-07.md`. Значения по умолчанию «Достигнута в полном объёме», «Выполнены в полном объёме» и выделение «улучшение» сохранены. Клинические числовые значения не подгоняются под эти формулировки.

| Пункт | Изменение |
|---|---|
| 1. Исходные оценки | Первая поздняя шкала/МКФ показывается текущей, исходная ячейка остаётся пустой. Исходные точки ограничены датой МДРК-1; восстановление из подходящей МДРК-1 сохранено. |
| 2. Импорт МДРК-2 | Шкалы добавляются в историю без удаления поздних оригиналов. МКФ объединяется по точкам: пустое не затирает заполненное, ранняя итоговая точка не заменяет позднюю. Пустые цель/потенциал не удаляют известный текст. |
| 3. План и выполнение | Назначенное количество/кратность отделены от отметок выполнения в модели, редакторе и МДРК-1. МДРК-2 и выписка считают даты внутри своих границ. Ручное количество не стирается при изменении периода; расхождение с отметками видно при проверке. |
| 4. Несколько листов | Все листы объединяются по процедуре, специалисту и длительности. Датированные повторы не удваивают счёт. Неопределимые количества нескольких недатированных листов не суммируются: остаётся пустое значение с предупреждением. ПКМ/«Источник» показывает участвующие файлы. |
| 5. Границы эпизода | Шкалы, МКФ и заключения до поступления исключены из текущих оценок. Старые анамнестические и диагностические тексты могут оставаться историей. Выполнения ограничиваются календарными днями госпитализации и датой снимка. |
| 6. Поля выписки | Режим, диета, риски, ограничения и потенциал выбираются из последних явно заполненных подходящих источников. Заключительная запись врача может дополнить итоговый статус и рекомендации МИС. Анамнестический список лекарств не превращается в итоговую терапию. Различающиеся сведения итоговых источников одной даты доступны для согласования. |
| 7. Конфликты МКФ | Разные оценки одной временной точки сохраняются как варианты с источниками. Выбор доступен через «Правка → Согласовать расхождения», хранится в черновике и привязан к дате. Новая оценка не получает старый выбранный балл при повторном сканировании. |
| 8. Факторы среды | Динамика учитывает знак квалификатора: усиление помощника — положительное изменение; усиление барьера — отрицательное. |

Один независимый review обнаружил смешение выбранного балла МКФ с датой нового источника при повторном сканировании. Исправлено и покрыто регрессией. Предложение считать конфликтом любые два измерения в один день отклонено: при разных явно заданных временах это могут быть две реальные оценки.

Проверка: полный pytest — **403 passed** (16 новых сценариев), compileall, privacy gate, git diff --check. На локальном примере: 12 исходников не изменены (SHA-256), 4 специалиста, 17 строк МКФ, 22 пары шкал, 8 объединённых процедур. Все 10 страниц сформированного DOCX просмотрены после рендеринга; таблицы не выходят за границы страницы. Два фрагмента одной процедуры с 7 и 2 разными датами выполнения объединены в 9. Отсутствующие повторные оценки остаются пустыми.

Изменён исходный код; Windows EXE не собирался и не проверялся. Версия и релиз не менялись. Проверка одного локального примера и синтетических сценариев не доказывает распознавание всех возможных форматов документов.
